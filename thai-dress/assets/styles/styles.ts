const wrapper = 'max-w-7xl lg:mx-auto p-5 md:px-10 w-full';
const flexStart = 'flex justify-start items-center';
const flexCenter = 'flex justify-center items-center';
const flexBetween = 'flex justify-between items-center';
const flexEnd = 'flex justify-end items-center';
const h1Bold = 'font-bold text-3xl lg:text-4xl';
const h2Bold = 'font-bold text-2xl lg:text-3xl';
const h3Bold = 'font-bold text-xl lg:text-2xl';

const styles: {
  wrapper: string;
  flexStart: string;
  flexCenter: string;
  flexBetween: string;
  flexEnd: string;
  h1Bold: string;
  h2Bold: string;
  h3Bold: string;
} = {
  wrapper,
  flexStart,
  flexCenter,
  flexBetween,
  flexEnd,
  h1Bold,
  h2Bold,
  h3Bold
};
export default styles;
